# AI-content-creation-automation
This project is a full-stack AI-powered content generation system that allows a user to enter a topic and automatically generate a long-form blog article in real time. It uses a React frontend for user interaction and a FastAPI backend for running a multi-step AI content pipeline.
